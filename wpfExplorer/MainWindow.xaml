<Controls:MetroWindow x:Class="wpfExplorer.MainWindow"
                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                      xmlns:Controls="http://metro.mahapps.com/winfx/xaml/controls"
                      Title="File Explorer" Height="600" Width="1000"
                      WindowStartupLocation="CenterScreen">

    <Controls:MetroWindow.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Controls.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Fonts.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Themes/Light.Blue.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Controls:MetroWindow.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Верхняя панель -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Grid.ColumnSpan="2" Background="{DynamicResource AccentColorBrush}">
            <TextBlock Text="File Explorer" Foreground="White" FontSize="24" VerticalAlignment="Center" Margin="10"/>
        </StackPanel>

        <!-- Дерево директорий -->
        <TreeView Name="DirectoryTree" Grid.Row="1" Grid.Column="0" SelectedItemChanged="DirectoryTree_SelectedItemChanged" Background="{DynamicResource AccentColorBrush4}">
            <!-- Пример данных для дерева -->
            <TreeViewItem Header="C:\" Tag="C:\" IsExpanded="True">
                <TreeViewItem Header="Program Files" Tag="C:\Program Files"/>
                <TreeViewItem Header="Users" Tag="C:\Users"/>
                <TreeViewItem Header="Windows" Tag="C:\Windows"/>
            </TreeViewItem>
        </TreeView>

        <!-- Список файлов -->
        <ListView Name="FileList" Grid.Row="1" Grid.Column="1" Background="{DynamicResource AccentColorBrush5}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" Width="300"/>
                    <GridViewColumn Header="Type" DisplayMemberBinding="{Binding Type}" Width="100"/>
                    <GridViewColumn Header="Size" DisplayMemberBinding="{Binding Size}" Width="100"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Controls:MetroWindow>